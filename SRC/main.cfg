MEMORY
{
    HEADER: start = $0000, size = $0010, file = %O, fill = yes;
    ROM_00: start = $8000, size = $4000, file = %O, bank = 0;
    ROM_01: start = $8000, size = $4000, file = %O, bank = 1;
    ROM_02: start = $8000, size = $4000, file = %O, bank = 2;
    ROM_03: start = $8000, size = $4000, file = %O, bank = 3;
    ROM_04: start = $8000, size = $4000, file = %O, bank = 4;
    ROM_05: start = $8000, size = $4000, file = %O, bank = 5;
    ROM_06: start = $8000, size = $4000, file = %O, bank = 6;
    ROM_07: start = $C000, size = $4000, file = %O, bank = 7;
}

SEGMENTS
{
    HEADER:        load = HEADER, type = ro;
    BANK_00_MAIN:  load = ROM_00, type = ro;
    BANK_00_VEC:   load = ROM_00, type = ro, start = $BFFA;
    BANK_01_MAIN:  load = ROM_01, type = ro;
    BANK_01_VEC:   load = ROM_01, type = ro, start = $BFFA;
    BANK_02_MAIN:  load = ROM_02, type = ro;
    BANK_02_VEC:   load = ROM_02, type = ro, start = $BFFA;
    BANK_03_MAIN:  load = ROM_03, type = ro;
    BANK_03_VEC:   load = ROM_03, type = ro, start = $BFFA;
    BANK_04_MAIN:  load = ROM_04, type = ro;
    BANK_04_VEC:   load = ROM_04, type = ro, start = $BFFA;
    BANK_05_MAIN:  load = ROM_05, type = ro;
    BANK_05_VEC:   load = ROM_05, type = ro, start = $BFFA;
    BANK_06_MAIN:  load = ROM_06, type = ro;
    BANK_06_VEC:   load = ROM_06, type = ro, start = $BFFA;
    BANK_07_MAIN:  load = ROM_07, type = ro;
    BANK_07_VEC:   load = ROM_07, type = ro, start = $FFFA;
}

